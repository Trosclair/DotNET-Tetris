<UserControl x:Class="WPFTetris.Views.MainMenu.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFTetris.Views.MainMenu" xmlns:viewmodels="clr-namespace:WPFTetris.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Column="1" Grid.Row="3" Content="Custom Game" Command="{Binding CustomGameSetupCommand}" Margin="50"/>
        <Button Grid.Column="2" Grid.Row="3" Content="Quick Game" Command="{Binding QuickGameCommand}" Margin="50"/>
        <Button Grid.Column="3" Grid.Row="3" Content="Options" Command="{Binding OptionsCommand}" Margin="50"/>
    </Grid>
</UserControl>
